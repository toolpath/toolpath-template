apps/*/build/
apps/*/.react-router/
apps/*/test-results/
apps/*/playwright-report/
packages/*/dist/

# pnpm owns the lockfile; Prettier re-wrapping it makes every install a diff.
pnpm-lock.yaml

# Generated by scripts/build-sample-catalog.mjs, and sample-catalog.test.ts holds
# it byte-for-byte to what the generator writes. Prettier re-wrapping it makes the
# committed file and the generator disagree.
packages/catalog-data/fixtures/

# Vendored viewer fixtures, kept identical to the DFM application's copies.
apps/catalog/tests/fixtures/
apps/dfm/tests/fixtures/

# A vendor's data, gitignored anyway.
scrape-out/
