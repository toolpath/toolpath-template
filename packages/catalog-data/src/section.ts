/**
 * A feature drawn in section, beside the tool that cuts it.
 *
 * **`@toolpath/tool-support`'s**, re-exported here. It is arithmetic over a
 * reach curve and a few dimensions — no part schema, no DOM — so it travelled
 * with the sweep it shares `heightAt` with.
 */

export {
  FLOOR_BAND,
  REACH,
  sectionOutline,
  type FeatureSection,
  type Section,
  type SectionKind,
  type SectionPoint,
} from '@toolpath/tool-support'
