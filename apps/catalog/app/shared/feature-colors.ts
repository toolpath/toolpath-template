/**
 * What roughing and finishing wear in the tool list.
 *
 * Two colours rather than an accent for both, so which pass the tool list is
 * answering for is readable without reading the label.
 *
 * **The part itself is not coloured.** It was, by feature type and by way up
 * and by what a feature was tooled for, and each of those was a question the
 * part was answering before anybody had asked it. What is on the part now is
 * what somebody clicked.
 */
export const PASS_COLORS = {
  rough: 0xd97706,
  finish: 0x7c3aed,
} as const
